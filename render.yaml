services:
  - type: web
    name: roommatch-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LITEAPI_KEY
        sync: false
      - key: NODE_ENV
        value: production
