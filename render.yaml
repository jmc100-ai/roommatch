services:
  - type: web
    name: roommatch-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: LITEAPI_KEY
        sync: false
      - key: NODE_ENV
        value: production
